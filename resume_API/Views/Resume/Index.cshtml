@using resume_MODELS.API

@model Root

@{
    ViewData["Title"] = "Index";
}

<h1><span class="text-light" >@(
          Model.basics.name.Split(" ")[0] + " "
        )</span><span>@(
        Model.basics.name.Split(" ")[1]
        )</span></h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.selectedTemplate)
            </th>
            <th>
                @Html.DisplayFor(model => model.selectedTemplate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.headings)
            </th>
            <th>
                @Html.DisplayFor(model => model.headings)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.basics)
            </th>
            <th>
                @Html.DisplayFor(model => model.basics)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.awards)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
    <tbody>
@foreach (var item in Model.awards) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
